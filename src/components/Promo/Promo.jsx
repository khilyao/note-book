import { Overlay, Modal, Section } from './Promo.styled';

const Promo = () => {
    return (
        <Section>
            <Overlay>
                <Modal></Modal>
            </Overlay>
        </Section>
    );
};

export default Promo;
